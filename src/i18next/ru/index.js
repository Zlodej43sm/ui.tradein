import { default as common } from './common.json';
import { default as auth } from './auth.json';
import { default as recover_password } from './recover_password.json';

export default { common, auth, recover_password };
