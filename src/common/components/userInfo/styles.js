export default (theme) => ({
  infoWrapper: {
    position: 'relative',
    marginLeft: theme.spacing(2)
  },
  paper: {
    position: 'absolute',
    width: 'max-content',
    top: 55,
    right: 0
  },
  icon: {
    minWidth: 40
  }
});
